                }
                else if(o[i] == "D"){
                else if(o[i] == "C"){
                    s.pop();
                }
                    s.push(2*s.top());
                }
                else{
                    s.push(stoi(o[i]));
                    int y = s.top();
                    s.push(x);
                    s.push(x+y);
                if(o[i] == "+"){
                    int x = s.top();
                    s.pop();
            if(s.empty()) s.push(stoi(o[i]));
            else{
    int calPoints(vector<string>& o) {
        stack<int> s;
        for(int i = 0; i < n;i++){
class Solution {
public:
        int n=o.size();
                }
[
